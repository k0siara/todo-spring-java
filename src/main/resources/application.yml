server:
  jackson:
    serialization:
      indent-output: true

mybatis:
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.patrykkosieradzki.todo: debug

db:
  url: ${JDBC_DATABASE_URL}
  username: ${JDBC_DATABASE_USERNAME}
  password: ${JDBC_DATABASE_PASSWORD}
  driver: org.postgresql.Driver